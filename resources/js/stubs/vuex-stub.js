/**
 * Stub temporal para vuex
 * Reemplazar gradualmente con @/stores/vuex-adapter
 */
export const mapState = () => ({})
export const mapActions = () => ({})
export const mapMutations = () => ({})
export const mapGetters = () => ({})

export default {
  mapState,
  mapActions,
  mapMutations,
  mapGetters
}

